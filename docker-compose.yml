version: '3'
services:
  tbl-local:
    image: victorhad/tbl:local
    container_name: tbl-local
    command: ./compose/local/dev.sh
    build:
      context: .
      dockerfile: ./compose/local/Dockerfile
    volumes:
      - .:/software
    ports:
      - "8000:8000"
